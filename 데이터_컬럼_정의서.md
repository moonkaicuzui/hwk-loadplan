# Rachgia Dashboard 데이터 컬럼 정의서

이 문서는 대시보드가 엑셀 데이터의 각 컬럼을 어떻게 해석하고 처리하는지 설명합니다.

---

## 1. Excel 컬럼 → 대시보드 매핑

### 기본 정보 컬럼 매핑

| Excel 컬럼 | 대시보드 필드 | 설명 |
|-----------|-------------|------|
| Unit | unit | 생산 단위 (예: RAF.01-SEW RA.01) |
| Season SPEC | season | 시즌 (예: SS26, FW25) |
| Model | model | 모델명 |
| Art | article | Article / PGSC 코드 |
| Color | color | 색상 코드 |
| Dest | destination | 행선지/국가 |
| Q.ty | quantity | 주문 수량 |
| SetP | poNumber | PO 번호 |
| CRD | crd | 고객요청일 (MM/DD 형식) |
| SDD (Original 또는 Current) | sddValue | SDD 날짜 |
| Outsole Vendor | outsoleVendor | 아웃솔 벤더 |

### 공정별 컬럼 매핑

| Excel 컬럼 | 대시보드 필드 | 공정명 |
|-----------|-------------|--------|
| S.Cut Bal | s_cut | 재단 |
| Pre-Sew Bal. | pre_sew | 재봉전 |
| Sew input Bal | sew_input | 재봉투입 |
| Sew\nBal | sew_bal | 재봉 |
| S/Fit Bal | s_fit | 스탁핏 (미드솔+아웃솔 접착 준비) |
| Ass Bal | ass_bal | 조립 |
| W.H IN BAL | wh_in | 창고입고 |
| W.H OUT BAL | wh_out | 창고출고 |

### 공정 컬럼 값 해석 로직

**중요**: 4개 공장 Excel 파일은 컬럼 구조가 서로 다릅니다. 대시보드는 공통 컬럼만 사용합니다.

| 공통 컬럼 | Factory A/B | Factory C/D |
|----------|-------------|-------------|
| Unit, Model, Art, Color, Dest, Q.ty | ✅ | ✅ |
| CRD, SDD | ✅ | ✅ |
| PRODUCTION STATUS (BAL 컬럼들) | ✅ | ✅ |
| WAREHOUSE (W.H IN/OUT BAL) | ✅ | ✅ |
| PD, CNF.SDD, UPC, DAILYSHEET | ❌ | ✅ |

Excel의 공정별 BAL 컬럼에는 두 가지 형태의 값이 들어옵니다:

```javascript
// 1. 날짜 형식 (예: "11/19", "12/01") → 미완료 상태
if (값이_날짜형식) {
    completed = 0;
    pending = quantity;  // 전체 수량이 미완료
    status = "pending";
    expected_date = 날짜값;  // ← BAL 컬럼의 날짜값을 저장
}

// 2. 숫자 형식 (예: 840, 500) → 완료/부분완료 상태
if (값이_숫자) {
    completed = 숫자값;
    pending = quantity - completed;
    expected_date = null;  // 완료되었으므로 예정일 없음

    if (completed >= quantity) {
        status = "completed";
    } else if (completed > 0) {
        status = "partial";
    } else {
        status = "pending";
    }
}
```

**참고**: `expected_date`는 별도의 Excel 컬럼이 아니라, BAL 컬럼 값이 날짜일 때 그 값을 저장한 것입니다.

---

## 2. 기본 정보 컬럼

### `factory` (공장)
- **타입**: String
- **예시**: `"A"`, `"B"`, `"C"`, `"D"`
- **대시보드 사용**:
  - 공장별 필터링
  - 공장별 완료율 계산
  - 공장별 카드 표시
  - Factory Radar 차트
  - 공장 순위 계산

### `poNumber` (PO 번호)
- **타입**: String
- **Excel 컬럼**: SetP
- **예시**: `"S11/5"`, `"S11/24"`
- **대시보드 사용**:
  - 오더 식별자
  - 통합 검색 대상
  - 모달 및 상세 뷰에서 표시
  - Excel 내보내기 포함

### `model` (모델명)
- **타입**: String
- **예시**: `"VL COURT 3.0 K"`, `"DURAMO SL 2.0"`, `"Tensaur Sport 2.0 CF I"`
- **대시보드 사용**:
  - 모델별 탭 차트/테이블
  - 모델별 필터링 (검색)
  - 히트맵 (모델 × 행선지, 모델 × 아웃솔 벤더)
  - 상세 모달 표시

### `article` (Article / PGSC)
- **타입**: String
- **Excel 컬럼**: Art
- **예시**: `"AB7-GW6468"`, `"AB7-GW6456"`
- **대시보드 사용**:
  - 상세 테이블에서 Article 컬럼으로 표시
  - 오더 상세 모달에서 "Article (PGSC)" 표시
  - 통합 검색 대상
  - **참고**: Article은 PGSC(Product Group Sub Category)와 동일한 의미로 사용됨

### `destination` (행선지/국가)
- **타입**: String
- **Excel 컬럼**: Dest
- **예시**: `"Japan"`, `"South Korea"`, `"Saudi Arabia"`, `"United Kingdom"`
- **대시보드 사용**:
  - 행선지별 필터
  - 중요 행선지 카드 (일본, 한국, 중국, 대만, 인도)
  - 행선지별 탭 차트/테이블
  - 히트맵 (국가 × 월, 모델 × 행선지)
  - **중요 행선지 매핑**:
    - `Japan` → 일본
    - `South Korea` / `Korea` → 한국
    - `China` → 중국
    - `Taiwan` → 대만
    - `India` → 인도

### `outsoleVendor` (아웃솔 벤더)
- **타입**: String
- **Excel 컬럼**: Outsole Vendor
- **예시**: `"HVC NT"`, `"Taesung CC"`, `"Samhwa CC"`
- **대시보드 사용**:
  - 아웃솔 벤더별 필터
  - 벤더 섹션 차트/테이블
  - 아웃솔 벤더 탭
  - 모델 × 아웃솔 벤더 히트맵
  - **빈 값 처리**: `null` 또는 빈 문자열은 `"(미지정)"`으로 표시

### `quantity` (주문 수량)
- **타입**: Number
- **Excel 컬럼**: Q.ty
- **예시**: `840`, `126`, `117`
- **대시보드 사용**:
  - 총 주문 수량 계산
  - 완료율 계산의 분모
  - 차트 데이터
  - 히트맵 셀 값
  - Excel 내보내기

---

## 3. 날짜 관련 컬럼

### `sddValue` (SDD)
- **타입**: String (날짜 형식)
- **Excel 컬럼**: SDD (Original 또는 Current)
- **예시**: `"2025.12.30"`, `"00:00:00"` (빈 값)
- **대시보드 사용**:
  - **기본 날짜 기준** (SDD 모드)
  - SDD 일자 초과 오더 판단 기준
  - SDD 3일 이내 오더 판단 기준
  - 캘린더 표시 기준
  - 월별 필터
  - Excel 내보내기
- **빈 값 처리**: `"00:00:00"` 또는 빈 문자열은 유효하지 않은 날짜로 처리

### `sddYearMonth` (SDD 연월)
- **타입**: String
- **예시**: `"2025-12"`, `"2026-01"`
- **대시보드 사용**:
  - 월별 필터 옵션 (SDD 모드)
  - 월별 차트/테이블
  - 히트맵 (국가 × 월)

### `crd` (CRD - 고객요청일)
- **타입**: String (날짜 형식)
- **Excel 컬럼**: CRD
- **예시**: `"01/15"`, `"12/31"` (MM/DD 형식)
- **대시보드 사용**:
  - **대체 날짜 기준** (CRD 모드 선택 시)
  - 상세 테이블에서 표시
  - 오더 상세 모달에서 표시
  - Excel 내보내기

### `crdYearMonth` (CRD 연월)
- **타입**: String
- **예시**: `"2025-12"`, `"2026-01"`
- **대시보드 사용**:
  - 월별 필터 옵션 (CRD 모드)
  - CRD 모드에서의 월별 분석

---

## 4. 생산 공정 데이터 (`production` 객체)

### 공정 순서
```
s_cut → pre_sew → sew_input → sew_bal → s_fit → ass_bal → wh_in → wh_out
재단  →  재봉전  →  재봉투입  →  재봉  → 스탁핏 →  조립  → 창고입고 → 창고출고
```

**스탁핏(s_fit)**: 미드솔과 아웃솔을 접착시키는 창 제조 준비 공정

### 각 공정의 구조
```javascript
production: {
    s_cut: { completed: Number, pending: Number, status: String, expected_date: String },
    pre_sew: { completed: Number, pending: Number, status: String, expected_date: String },
    sew_input: { completed: Number, pending: Number, status: String, expected_date: String },
    sew_bal: { completed: Number, pending: Number, status: String, expected_date: String },
    s_fit: { completed: Number, pending: Number, status: String, expected_date: String },
    ass_bal: { completed: Number, pending: Number, status: String, expected_date: String },
    wh_in: { completed: Number, pending: Number, status: String, expected_date: String },
    wh_out: { completed: Number, pending: Number, status: String, expected_date: String }
}
```

### `completed` (완료 수량)
- **타입**: Number
- **예시**: `840`, `0`
- **의미**: 해당 공정에서 완료된 수량

### `pending` (미완료 수량)
- **타입**: Number
- **예시**: `0`, `840`
- **의미**: 해당 공정에서 아직 처리되지 않은 수량

### `status` (공정 상태)
- **타입**: String
- **값**:
  - `"completed"`: 전체 완료 (completed >= quantity)
  - `"partial"`: 부분 완료 (completed > 0 AND completed < quantity)
  - `"pending"`: 미시작 (completed = 0)
- **대시보드 표시**:
  - `completed` → ✅
  - `partial` → 🔄
  - `pending` → ⏳

### `expected_date` (예정일) - 파생 필드
- **타입**: String (날짜 형식) 또는 `null`
- **예시**: `"2025-11-19"`, `null`
- **출처**: Excel의 BAL 컬럼 값이 날짜 형식일 때 추출
- **의미**: 해당 공정의 예정 완료일 (BAL 컬럼이 숫자인 경우 `null`)
- **주의**: 별도의 Excel 컬럼이 아닌, BAL 컬럼에서 파생된 값

---

## 5. 대시보드 계산 로직

### 완료율 계산 (v5 기준: 창고입고 기준)

```javascript
// 전체 완료율 = 총 창고입고 완료 수량 / 총 주문수량
전체_완료율 = (총_창고입고_completed / 총_quantity) × 100

// 공정별 완료율
공정_완료율 = (해당공정_completed / (해당공정_completed + 해당공정_pending)) × 100
```

### SDD 일자 초과 오더 (지연 오더)

**정의**: SDD 날짜가 오늘보다 이전인데 아직 창고입고가 완료되지 않은 오더

```javascript
function isDelayed(order) {
    const dateValue = dateMode === 'sdd' ? order.sddValue : order.crd;

    // 1. 날짜 값이 없거나 유효하지 않으면 지연 아님
    if (!dateValue || dateValue === '00:00:00') return false;

    // 2. 창고입고 완료 수량 >= 주문 수량이면 지연 아님 (완료됨)
    if (wh_in_completed >= quantity) return false;

    // 3. SDD(또는 CRD)가 오늘보다 이전이면 지연
    return targetDate < today;
}
```

**대시보드 표시**: 🚨 아이콘, 빨간색 배경, "SDD 일자 초과" 카운트

### SDD 3일 이내 오더 (경고 오더)

**정의**: SDD 날짜가 오늘부터 3일 이내인데 아직 창고입고가 완료되지 않은 오더

```javascript
function isWarning(order) {
    const dateValue = dateMode === 'sdd' ? order.sddValue : order.crd;

    // 1. 날짜 값이 없거나 유효하지 않으면 경고 아님
    if (!dateValue || dateValue === '00:00:00') return false;

    // 2. 창고입고 완료되었으면 경고 아님
    if (wh_in_completed >= quantity) return false;

    // 3. 오늘부터 3일 이내에 예정되어 있으면 경고
    const diffDays = (targetDate - today) / (1000 * 60 * 60 * 24);
    return diffDays >= 0 && diffDays <= 3;
}
```

**대시보드 표시**: ⚡ 아이콘, 노란색 배경, "SDD 3일 이내" 카운트

### 창고 재고 계산

```javascript
// 창고 재고 = 창고입고 완료 - 창고출고 완료
function getInventory(order) {
    const whIn = order.production?.wh_in?.completed || 0;
    const whOut = order.production?.wh_out?.completed || 0;
    return whIn - whOut;  // 양수면 재고 있음
}
```

### 병목 공정 판단

```javascript
// 가장 낮은 완료율을 가진 공정이 병목
function findBottleneck(orders) {
    let minRate = 100;
    let bottleneckProcess = null;

    PROCESS_ORDER.forEach(process => {
        const rate = calculateProcessRate(orders, process);
        if (rate < minRate) {
            minRate = rate;
            bottleneckProcess = process;
        }
    });

    return { process: bottleneckProcess, rate: minRate };
}
```

---

## 6. SDD vs CRD 비교

| 항목 | SDD | CRD |
|------|-----|-----|
| 의미 | Scheduled Delivery Date | Customer Request Date |
| 용도 | 공장 출고 예정일 | 고객 요청 납기일 |
| 기준 | 생산/물류 관점 | 고객/영업 관점 |
| 날짜 비교 | SDD ≤ CRD (일반적) | 실제 고객 납기 |

**중요**: SDD가 12월 7일이고 CRD가 12월 30일인 경우, 12월 30일에 출고되었다면 SDD 기준으로는 지연이지만 CRD 기준으로는 정시 출고입니다. 따라서 어떤 날짜 기준을 선택하느냐에 따라 지연/경고 오더 수가 달라집니다.

---

## 7. 추가 컬럼 (선택적)

### `color` (색상)
- **타입**: String
- **예시**: `"FTWR WHITE/TEAM RE"`, `"CORE BLACK/FTWR WH"`
- **대시보드 사용**: 오더 상세 모달에서 표시

### `season` (시즌)
- **타입**: String
- **예시**: `"SS26"`, `"FW25"`
- **대시보드 사용**: 오더 상세 모달에서 표시

### `unit` (단위/라인)
- **타입**: String
- **예시**: `"RAF.01-SEW RA.01"`
- **대시보드 사용**: 오더 상세 모달에서 표시

---

## 8. 데이터 흐름 요약

```
Excel 파일 (4개 공장)
    ↓
파싱 및 변환
    ↓
┌─────────────────────────────────────────────────────────┐
│  각 공정 BAL 컬럼 값 해석:                               │
│  - 날짜 형식 → pending 상태, expected_date 설정         │
│  - 숫자 형식 → completed 수량, status 계산              │
└─────────────────────────────────────────────────────────┘
    ↓
JSON 변환 (EMBEDDED_DATA)
    ↓
대시보드 렌더링
    - 기본 정보 → 필터, 테이블, 모달
    - 날짜 정보 → SDD 초과/3일 이내 오더 판단
    - 공정 정보 → 완료율, 병목 분석, 상태 아이콘
```

---

## 9. v5 변경사항 요약

| 항목 | v4 | v5 |
|------|-----|-----|
| 완료율 기준 | 창고출고 (wh_out) | 창고입고 (wh_in) |
| 날짜 기준 | SDD 고정 | SDD/CRD 선택 가능 |
| Vendor 표기 | Vendor | 아웃솔 벤더 |
| 테이블 정렬 | 일부 지원 | 모든 테이블 헤더 클릭 정렬 |
| 모달 정보 | 기본 정보 | PO, Article, CRD, SDD 추가 |
| 오더 클릭 | 없음 | 공정별 상세 현황 시각화 |
| 히트맵 | 기본 | 모델×아웃솔 벤더 히트맵 추가 |
| 공정명 | 샘플핏 | 스탁핏 (정확한 명칭) |

---

## 10. 검증 체크리스트

엑셀 파일을 직접 확인할 때 다음 사항을 검증하세요:

### 기본 데이터 검증
- [ ] `Q.ty` 값이 숫자로 올바르게 파싱되는가?
- [ ] `SDD` 날짜가 `YYYY.MM.DD` 형식인가? (예: 2025.12.30)
- [ ] `CRD` 날짜가 `MM/DD` 형식인가? (예: 01/15)
- [ ] `Outsole Vendor` 값이 올바르게 매핑되는가?

### 공정 데이터 검증
- [ ] BAL 컬럼 값이 날짜(MM/DD)인 경우 → pending 상태로 변환되었는가?
- [ ] BAL 컬럼 값이 숫자인 경우 → completed 수량으로 변환되었는가?
- [ ] `completed + pending = quantity`가 성립하는가?
- [ ] `status` 값이 `completed`, `partial`, `pending` 중 하나인가?

### 계산 로직 검증
- [ ] SDD 일자 초과 오더: SDD < 오늘 AND 창고입고 미완료인 오더만 카운트되는가?
- [ ] SDD 3일 이내 오더: 0 ≤ (SDD - 오늘) ≤ 3일 AND 창고입고 미완료인 오더만 카운트되는가?
- [ ] 창고 재고: wh_in.completed - wh_out.completed 계산이 맞는가?

---

*문서 작성일: 2025-12-21*
*대시보드 버전: v5.1*
